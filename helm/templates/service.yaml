apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticketcraft.fullname" . }}
  labels:
    {{- include "ticketcraft.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: https
  selector:
    {{- include "ticketcraft.selectorLabels" . | nindent 4 }}
